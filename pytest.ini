[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --verbose --cov=prompter --cov-report=term-missing
markers =
    slow: mark test as slow to run
    integration: mark test as an integration test
    unit: mark test as a unit test 